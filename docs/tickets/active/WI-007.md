# WI-007 — Fix Add sheet content ghosting above the header when the keyboard opens

Status: Ready

Spec: `docs/specs/add-sheet-keyboard-open-ghosting.md`

Codex profile: sol-high

Profile reason: confirmed real bug, but the exact rendering mechanism is
not yet understood (see spec's Investigation section) and a desktop-only
verification path has already been ruled out — this is difficult
debugging against a real-device-only repro, matching `sol-high`'s
"difficult debugging... any task whose failure mode isn't yet well
understood" criterion, not routine UI work.

## Goal

Eliminate the transient (~1-1.5s) glitch where the Add sheet's Type field
content renders above the sheet's own rounded top edge and above the
sticky header, visible while the on-screen keyboard is opening after the
Note field is focused.

## Acceptance criteria

- [ ] Root cause identified via real-device inspection (remote debugging
      or equivalent) — not just re-confirmed to occur, but understood well
      enough to explain why the fix works.
- [ ] Reproduced and confirmed fixed on a real device: open the Add sheet,
      focus the Note field, keyboard opens — no content ever paints above
      the sheet's rounded top edge at any point during the transition.
      Repeat several times (timing-sensitive bugs can be intermittent).
- [ ] The existing keyboard-avoidance behavior `syncSheetToViewport()` was
      built for (sheet no longer gets dragged off-screen by the browser's
      native scroll-into-view on a short viewport — see
      `docs/CHANGELOG.md`'s "On-screen keyboard pushing the whole sheet
      off-screen" entry) still works, confirmed on the same real device.
- [ ] `wireSheetDrag()`'s swipe-to-dismiss gesture and scroll-lock/focus-trap
      behavior (`createFocusTrap()`, shared by all six sheets) are
      unaffected on every sheet, not just Add.
- [ ] Fix does not depend on desktop-only verification — the spec's
      Investigation section already confirmed desktop resize simulation
      does not reproduce this bug.

## Verification

- [ ] `npm test`
- [ ] `npm run test:e2e`
- [ ] `npm run build`
- [ ] Real-device check per the spec's Verification plan (mandatory —
      this bug cannot be confirmed fixed any other way).

## Out of scope

- The Type-selector's icon/color redesign (WI-006, separate ticket and
  root cause).
- Any other sheet's own field ordering/content.

## Dependencies

None.

## Review notes

Record confirmed defects and their resolution here. Keep optional suggestions
clearly separate.
