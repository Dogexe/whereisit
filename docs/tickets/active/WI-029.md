# WI-029 — Make a released overlay entry stop consuming the entry below it

Status: Ready

<!--
Written after Codex escalated WI-028 rather than implementing it, and after
the defect was reproduced independently against the merged
src/overlay-history.js. This ticket exists only because WI-028 cannot be
built correctly on the shipped module; it is a prerequisite, dispatched
before WI-028 despite the higher number.
-->

Spec: `docs/specs/back-button-dismisses-overlays.md`

Codex profile: sol-high

Profile reason: four lines of code, but the reasoning is the whole ticket —
asynchronous `popstate`, a self-inflicted history traversal, and one shared
module that all five shipped consumers depend on. A wrong fix here is a silent
regression across the entire workstream, not a visible bug.

Risk tier: Medium

<!--
Medium: no persisted state, no sync, no schema, no mapper, and no visible
change. Not Low because it edits the one module WI-026/WI-027 built the
workstream on.
-->

## Goal

`releaseOverlayHistory()` stops closing the overlay underneath the one being
released, so WI-028 can stack the Settings sub-page and the Manage sheet.

## The defect

`releaseOverlayHistory()` (`src/overlay-history.js:16-22`) pops its own entry
and then calls `history.back()`. The module's single `popstate` listener
(`:3-7`) pops **unconditionally**, so the traversal that release just caused
consumes the *next* entry down and fires its `onPop`. Reproduced against the
merged module with stubbed `window`/`history`:

```
push("settings-subpage", …); push("settings-manage", …);
releaseOverlayHistory("settings-manage");
-> fired: [ 'subpage onPop' ]   history entries: 1
```

Both entries are gone and the lower overlay closed itself. This is latent, not
live: none of the five WI-026/WI-027 consumers can be open at the same time
(WI-027's review notes verify the no-co-open claim), so nothing stacks two
entries today. WI-028 is the first ticket that does, which is why it escalated.

The spec's "Release" bullet and its decision 2 both say the resulting pop
"finds nothing" — true only for a stack of one. Both are corrected in the same
change as this ticket, along with WI-028's inherited copy of the claim.

## Acceptance criteria

- [ ] With `[A, B]` on the stack, `releaseOverlayHistory("B")` leaves `A` on the
      stack, does **not** call `A`'s `onPop`, and leaves `A`'s history entry in
      place. A subsequent user Back then closes `A` normally.
- [ ] A user Back still pops the top entry and fires its `onPop` exactly as
      today, for a stack of one and a stack of two.
- [ ] The suppression consumes **exactly one** `popstate` per release. A Back
      immediately after a non-Back dismissal is not swallowed — this is the
      failure mode that would break all five shipped sheets, and it is the one
      thing to test hardest.
- [ ] Releasing two entries in the same task leaves no leftover suppression that
      eats a later user Back (see Implementation guidance — a counter, not a
      boolean, and confirm it drains).
- [ ] `releaseOverlayHistory()` still returns `false` and does nothing when
      `key` is not the top entry; `pushOverlayHistory()` is unchanged, including
      its duplicate-key no-op and its `{ overlay: key }` tag.
- [ ] The two exported signatures and their return values are unchanged. **No
      consumer file is touched** — `add.js`, `transactions.js`, `insights.js`,
      `export-sheet.js`, `import-sheet.js` and `settings.js` are all untouched
      by this ticket.
- [ ] `tests/overlay-history.test.js` exists and covers the stacked release, the
      ordinary Back, and the not-swallowed-Back case.
- [ ] `e2e/overlay-history.spec.js` and `e2e/nav.spec.js` pass **unchanged**.
- [ ] No new CSS, class, `STRINGS` key, icon, or visible control.

## Implementation guidance

- **Likely files:** `src/overlay-history.js` (22 lines — read it first, it is
  shorter than this section), a new `tests/overlay-history.test.js`, and the
  doc corrections listed under Verification.
- **Suggested shape** — suppress exactly the pops release causes:

  ```js
  let suppressedPops = 0;

  window.addEventListener("popstate", () => {
    if (suppressedPops > 0) { suppressedPops--; return; }
    const entry = overlayStack.pop();
    if (!entry) return;
    entry.onPop();
  });

  // in releaseOverlayHistory, replacing the bare history.back():
  overlayStack.pop();
  suppressedPops++;
  history.back();
  ```

  Deviate only with a reason recorded in Review notes.
- **A counter, not a boolean.** Same size, correct on the edge case a boolean
  gets wrong: two releases in one task queue two traversals, and a boolean would
  swallow one popstate and let the other close a live overlay.
- **`popstate` is asynchronous.** The suppression cannot be a call-stack guard or
  a `try/finally` — `history.back()` returns immediately and the event arrives on
  a later task. That is exactly why a module-level counter is the shape, and why
  the unit test's stubbed `back()` must invoke the listener the way the real one
  does.
- **Reason about a release whose `back()` produces no `popstate`.** If that can
  happen the counter never drains and the user's next Back is swallowed. Every
  entry release drops was pushed by `pushOverlayHistory`, so the traversal should
  always have somewhere to go — confirm that rather than assume it, and say so in
  Review notes.
- **Do not dispatch on `event.state` instead.** That was considered and rejected
  in the spec's decision 1: `main.js:73`/`:109` can `replaceState` the tag away,
  so the stack is the source of truth and the tag is identification only.
- **Unit test shape.** The module registers its `popstate` listener at import
  time, so the stubs must be installed on `globalThis` *before* a dynamic
  `import()` of it — the rest of `tests/` imports statically, so this one file
  will look different on purpose; say why in a comment. A minimal
  `window.addEventListener` that captures the listener, plus a `history` with
  `pushState`/`back()` that invokes it, is enough.
- **The unit test is the real gate here, and that is deliberate.** No UI path can
  stack two entries until WI-028 ships, so e2e cannot reach the defect.
  `npm run test:e2e` covers the other half — that the five shipped sheets did not
  regress.
- **Do not change:** the exported API, the `{ overlay: key }` tag, the
  duplicate-key no-op, any consumer, or `settings.js`'s own `popstate` listener
  (WI-028 deletes that one, not this ticket).
- **Escalate back to Claude if:**
  - the fix appears to require an API change or any consumer edit;
  - a released `history.back()` can fail to produce a `popstate`, leaving the
    counter stuck;
  - browsers coalesce two queued traversals into a single `popstate`, which would
    make the counter over-suppress.

## Verification

- [ ] `npm test` — including the new `tests/overlay-history.test.js`.
- [ ] `npm run test:e2e` — the full suite unchanged; `overlay-history.spec.js`
      and `nav.spec.js` are the regression guards for the five shipped sheets.
- [ ] `npm run build`
- [ ] `docs/specs/back-button-dismisses-overlays.md` corrected in the same
      change: the "Release" bullet (`:69-72`) and decision 2 (`:196-198`) both
      claim the release-triggered pop "finds nothing", which holds only for a
      stack of one.
- [ ] `docs/ARCHITECTURE.md` — check whether it describes the owner's pop
      behavior; update it if it does. Do **not** touch the `:71-73` sentence
      about `settings.js`, which is WI-028's to change.
- [ ] Complete diff inspected for unrelated changes.

## Out of scope

- WI-028 itself — the Settings sub-page and Manage sheet migration.
- Adopting any new consumer.
- Tab switches pushing history.
- Any URL, route, or deep-link change.

## Dependencies

**WI-026** — satisfied. Shipped and merged to `main`.

**WI-027** — satisfied in content, but **not yet merged**: it is committed on the
local branch `wi-027/overlay-history-adoption` and was never pushed. Branch this
ticket off whatever WI-027 lands on, so the e2e regression guards run against the
four sheets it adopted.

## Review notes

Record confirmed defects and their resolution here. Keep optional suggestions
clearly separate.
